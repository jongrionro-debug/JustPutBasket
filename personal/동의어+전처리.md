1. feature slot 먼저 고정
category
silhouette
color
material
pattern
texture
mood
season
era
detail
-> 이 10개의 축을 그대로 사용한다. 
2. 각 이미지마다 llm이 아래의 내용을 뱉게 설정(작은 데이터셋으로 진행)
각 slot별 값, 짧은 caption, 불확실하면 빈칸으로 뱉도록 설정
3. slot 별 value 수집하기
각 slot별 vlaue들을 기반으로 frequency table만들기
4. 이 value가 동의어 인가? 아니면 부모 종속 관계인가??
5. canconical label 초안 만들기 
feature별로 표 만들기 
feature|canonical|variants|mapping_type|notes
6. 초안에 대해서 raw tags 전체에 canonical mapping 적용하기-> 여기서 raw feature, canonical feature둘다 저장을 해서 나중에 canonical feature를 바꾸더라도 다시 돌릴 수 있게 두자.(분리 해서 저장하기)
7. 이제 retrieval 품질 살펴보기


###### In more detail
1. 어떤 모양으로 데이터 단위를 확정 시킬까??
image_id, file_path, year, season, brand, source split으로 한 row구성
브랜드, 시즌, year은 파일 경로에서 자동 추출 가능
2. 각 브랜드에 대해서 100장(min(x,100))씩 뽑아서 llm rough tagging 진행. local clip류 model로 진행
3.출력 칼럼은 image_id, raw_category, raw_color, raw_material,raw_mood,tagging_confidence 출력
4. feature 별로 분리해서 빈도표 만들자.
5. canonical table 만들기
feature, canonical, variant,mapping type= synonym or parent_map
6. raw feautre들, canonical feature들 같이 저장하자. 
7. retrieval 로그 비교하기 

